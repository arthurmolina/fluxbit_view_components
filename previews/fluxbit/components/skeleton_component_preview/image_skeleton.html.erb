<div class="space-y-6">
  <div>
    <h4 class="mb-4 text-lg font-semibold text-gray-900 dark:text-white">Small Image</h4>
    <%= fx_skeleton(variant: :image, size: :small) %>
  </div>

  <div>
    <h4 class="mb-4 text-lg font-semibold text-gray-900 dark:text-white">Medium Image (Default)</h4>
    <%= fx_skeleton(variant: :image, size: :medium) %>
  </div>

  <div>
    <h4 class="mb-4 text-lg font-semibold text-gray-900 dark:text-white">Large Image</h4>
    <%= fx_skeleton(variant: :image, size: :large) %>
  </div>
</div>