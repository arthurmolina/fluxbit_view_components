<div class="space-y-6">
  <div>
    <h4 class="mb-4 text-lg font-semibold text-gray-900 dark:text-white">Small Video</h4>
    <%= fx_skeleton(variant: :video, size: :small) %>
  </div>

  <div>
    <h4 class="mb-4 text-lg font-semibold text-gray-900 dark:text-white">Medium Video (Default)</h4>
    <%= fx_skeleton(variant: :video, size: :medium) %>
  </div>

  <div>
    <h4 class="mb-4 text-lg font-semibold text-gray-900 dark:text-white">Large Video</h4>
    <%= fx_skeleton(variant: :video, size: :large) %>
  </div>
</div>