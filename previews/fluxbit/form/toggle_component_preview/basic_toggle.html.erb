<div class="space-y-4">
  <%= fx_toggle(
    label: "Dark mode",
    name: "dark_mode"
  ) %>

  <%= fx_toggle(
    label: "Auto-save",
    name: "auto_save",
    checked: true
  ) %>

  <%= fx_toggle(
    label: "Show notifications",
    name: "notifications",
    help_text: "Enable to receive alerts"
  ) %>
</div>